using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;

public class Converter : MonoBehaviour
{
    public static bool gameStarted;

    private void Awake()
    {
        // string jsonPath = "Assets/Resources/ResourceInfo.json";
        // string textPath = "Assets/Resources/ResourceInfo.txt";
        // File.WriteAllText(textPath, string.Empty);
        // StreamWriter writer = new StreamWriter(textPath, true);
        // TextAsset infoJSON = Resources.Load<TextAsset>("ResourceInfo.json");

        // Debug.Log("Writing Text Asset");
    }

    private void Start()
    {

    }
}